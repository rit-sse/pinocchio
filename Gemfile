source 'http://rubygems.org'

gem 'sinatra', require: "sinatra/base"
gem 'redis'
gem 'sinatra-flash'
gem 'redis-rack'

group :production do
  gem 'unicorn'
  gem 'whiskey_disk'
end

# gem 'rack-flash'
# gem 'rack', "1.3.6" # currently v1.3.6 is necessary for rack-flash to work
